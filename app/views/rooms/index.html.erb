<%# もしユーザーがルームなし状態なら見せる。ルームがあればルームメンバーを見せる %>
<% if user_signed_in? %>
  <% if !Room.exists?(user_id = current_user.id)%>
    <%= link_to "ルームを作る",  new_room_path %>
    or
    <%= link_to "ルームを探す",serach_rooms_path %>
  <%else%>
    <%= link_to "ルームに入る",room_menus_path(@room)%>
  <% end %>
<%else%>
  ログインして始めましょう
<%end%>


